diff -drupN a/drivers/input/keyboard/Makefile b/drivers/input/keyboard/Makefile
--- a/drivers/input/keyboard/Makefile	2018-08-06 17:23:04.000000000 +0300
+++ b/drivers/input/keyboard/Makefile	2022-06-12 05:28:14.000000000 +0300
@@ -64,3 +64,7 @@ obj-$(CONFIG_KEYBOARD_TEGRA)		+= tegra-k
 obj-$(CONFIG_KEYBOARD_TWL4030)		+= twl4030_keypad.o
 obj-$(CONFIG_KEYBOARD_XTKBD)		+= xtkbd.o
 obj-$(CONFIG_KEYBOARD_W90P910)		+= w90p910_keypad.o
+obj-$(CONFIG_KEYBOARD_SUNXI)		+= sunxi-keyboard.o
+obj-$(CONFIG_IR_TX_SUNXI)		+= sunxi-ir-tx.o
+obj-$(CONFIG_GPIO_IR_RX_SUNXI)		+= sunxi-gpio-ir-rx.o
+obj-$(CONFIG_GPIO_IR_TX_SUNXI)		+= sunxi-gpio-ir-tx.o
