# The kernel module reads /etc/hgicf.conf at insmod time (conf_file default).
# Set both explicitly so behaviour is clear regardless of load mechanism.
options hgicf fw_file=taixin/hgicf.bin conf_file=/etc/hgicf.conf
