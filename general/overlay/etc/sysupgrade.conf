# sysupgrade keep-list — extra paths to preserve across upgrades.
# One path per line; files or directories. Lines starting with '#' are ignored.
# Defaults already preserved: /etc/passwd /etc/shadow /etc/group /etc/dropbear
# /etc/network /etc/majestic.yaml /etc/wpa_supplicant.conf /etc/wireguard.conf
# /etc/sysctl.conf /etc/ntp.conf /etc/timezone /etc/localtime /root
